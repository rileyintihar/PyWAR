# Adds new player to player database

class Player:
    def __init__(self, name, number, throw, bat):
        self.name = name
        self.number = number
        self.throw = throw
        self.bat = bat